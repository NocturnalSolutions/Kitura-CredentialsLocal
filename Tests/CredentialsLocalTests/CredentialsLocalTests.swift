import XCTest
@testable import Kitura_CredentialsLocal

class Kitura_CredentialsLocalTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Kitura_CredentialsLocal().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
